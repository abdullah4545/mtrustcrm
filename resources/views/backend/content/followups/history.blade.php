@extends('backend.master')
@section('title','Follow-up History')
@section('maincontent')<div class="nxl-content"><div class="page-header"><div><h4>Follow-up History - {{ $lead->lead_no }}</h4><small>{{ $lead->person_name }} · {{ $lead->organization->name ?? '-' }}</small></div></div></div><div class="main-content"><div class="card"><div class="card-body"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Date & Time</th><th>Created By</th><th>Action</th><th>Message / Note</th><th>Outcome</th><th>Next Follow-up</th><th>Relation</th></tr></thead><tbody>@forelse($items as $r)<tr><td>{{ optional($r->activity_at)->format('d M Y h:i A') }}</td><td>{{ $r->creator->name ?? '-' }}</td><td>{{ ucfirst($r->activity_type) }}</td><td>{{ $r->activity_text ?: '-' }}</td><td>{{ $r->outcome_status ?: '-' }}</td><td>{{ optional($r->next_followup_at)->format('d M Y h:i A') ?: '-' }}</td><td>@if($r->rescheduled_from_id)<span class="badge bg-info">Rescheduled from #{{ $r->rescheduled_from_id }}</span>@else-@endif</td></tr>@empty<tr><td colspan="7" class="text-center">No history</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</div></div></div>@endsection
